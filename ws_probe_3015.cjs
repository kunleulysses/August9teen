const WebSocket=require('ws');const fs=require('fs');const t=fs.readFileSync('/opt/featherweight/ws_tok.txt','utf8').trim();const ws=new WebSocket('ws://127.0.0.1:3015/consciousness-stream', t);let first=true;ws.on('open',()=>console.log('OPEN'));ws.on('message',m=>{ if(first){ first=false; console.log('MSG', m.toString().slice(0,180)); ws.close(); }});ws.on('error',e=>{ console.log('ERR', e.message); process.exit(2); });ws.on('close',()=>{ console.log('CLOSE'); process.exit(0); });setTimeout(()=>{ console.log('TIMEOUT'); process.exit(3); }, 7000);
