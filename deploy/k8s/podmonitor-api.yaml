apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: api-podmonitor
  namespace: consciousness
spec:
  selector:
    matchLabels:
      app: consciousness-api
  podMetricsEndpoints:
    - port: 3000
      path: /metrics
      interval: 15s