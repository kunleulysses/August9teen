name: Chaos Suite
on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 5'
jobs:
  k6_chaos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run k6 test
        uses: grafana/k6-action@v0.2.0
        with:
          filename: tests/perf/holo-k6-load.js